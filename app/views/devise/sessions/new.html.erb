<%= form_for(resource, :as => resource_name,
             :url => session_path(resource_name),
             :html => { :class => 'form-horizontal' }) do |f| %>
  <%= devise_error_messages! %>
  <%= f.text_field :login, :required => true, :autofocus => true, :placeholder => 'Email ou Nome do Usuário', :class => 'devise' %>
  <%= f.password_field :password, :required => true, :placeholder => 'Senha', :class => 'devise' %>
  <%= f.submit 'Logar', :class => 'btn btn-large btn-block btn-primary' , :id => :btn_login %>
<% end %>
<%= render "devise/shared/links" %>